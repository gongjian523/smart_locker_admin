<UserControl x:Class="UboConfigTool.Modules.User.View.VersionsSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,10,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <RadioButton x:Name="radsystemAdmin" Content="固件版本" Style="{StaticResource TabRadioButton}" IsChecked="True" GroupName="adminGroup"/>
                <RadioButton x:Name="radLogAdmin" Margin="40,0,0,0"  Style="{StaticResource TabRadioButton}"   GroupName="adminGroup" Content="病毒库版本"/>
            </StackPanel>
            <Button Width="100" Content="+添加版本" Height="32" Margin="0,10,0,0" HorizontalAlignment="Right" Style="{DynamicResource uboBlackSquareButtonStyle}"/>
        </Grid>
        
        <Grid Margin="0,10,0,0" Grid.Row="1" Background="White" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
    </Grid>
</UserControl>
