<UserControl x:Class="CFLMedCab.View.Stock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CFLMedCab.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" Width="800" MinWidth ="800">
    <Grid Background="AliceBlue">
        <Grid Margin="10,10,10,387" Background="White">
            <RadioButton Content="库存快照" GroupName="menu1" Name="stockSnapshot" FontSize="17" Style="{DynamicResource FrameNavButton}"  HorizontalAlignment="Left" Margin="20,18,0,0" VerticalAlignment="Top" IsChecked="True" Height="35" FontWeight="Bold" Click="StockSnapshot"/>
            <RadioButton Content="效期查询" GroupName="menu1" Name="validity" FontSize="17" Style="{DynamicResource FrameNavButton}"  HorizontalAlignment="Left" Margin="130,18,0,0" VerticalAlignment="Top" Height="35" FontWeight="Bold" Click="EffectivePeriod"/>
            <RadioButton Content="出入库记录查询" GroupName="menu1" Name="stock" FontSize="17" Style="{DynamicResource FrameNavButton}"  HorizontalAlignment="Left" Margin="240,18,0,0" VerticalAlignment="Top" Height="35" FontWeight="Bold" Click="StockQuery" />
        </Grid>
        <Grid Background="White" x:Name="gChoice" RenderTransformOrigin="0.497,0.507" Margin="10,78,10,324">
            <RadioButton  Content="库存快照" GroupName="Stock" IsChecked="True" Style="{DynamicResource CircularNavButton}"  HorizontalAlignment="Left" Margin="20,10,0,0" VerticalAlignment="Top" Click="StockSnapshot"/>
            <RadioButton  Content="条件查询" Name="rbQuery" GroupName="rbQuery" IsChecked="True" HorizontalAlignment="Left" Margin="139,10,0,0" VerticalAlignment="Top" Style="{DynamicResource CircularNavButton}"  Click="ConditionQuery" />
            <RadioButton GroupName="single" Name="single1" Visibility="Hidden" Style="{DynamicResource SingleNavButton}" Margin="129,-38,514,8" Content="效期小于1个月" />
            <RadioButton GroupName="single" Name="single2" Visibility="Hidden" Style="{DynamicResource SingleNavButton}" Margin="282,-38,341,8" Content="效期小于2个月" />
            <RadioButton GroupName="single" Name="single3" Visibility="Hidden" Style="{DynamicResource SingleNavButton}" Margin="444,-38,179,8" Content="效期小于3个月" />
        </Grid>
        <Grid Background="White" x:Name="gChoice1" RenderTransformOrigin="0.497,0.507" Margin="10,78,10,324" Visibility="Hidden">
            <RadioButton GroupName="single" Name="out" Style="{DynamicResource SingleNavButton}" IsChecked="True"  Margin="-610,-38,0,0" Content="出库" />
            <RadioButton GroupName="single" Name="in"  Style="{DynamicResource SingleNavButton}" Margin="-300,-38,0,0" Content="入库" />
        </Grid>
        <Grid Background="White" Name="gQuery" HorizontalAlignment="Left" Height="50" Margin="10,126,0,0" VerticalAlignment="Top" MinWidth="780" Visibility="Hidden" >
            <TextBox HorizontalAlignment="Left" Height="30" Margin="20,10,0,-24" Grid.Row="1" TextWrapping="Wrap" Text="输入商品编码" VerticalAlignment="Top" MinWidth="275" Foreground="#999999" FontWeight="Bold" FontSize="14"/>
            <TextBox HorizontalAlignment="Left" Height="30" Margin="325,9,0,0" TextWrapping="Wrap" Text="输入商品名称" VerticalAlignment="Top" MinWidth="275" Foreground="#999999" FontSize="14" FontWeight="Bold"/>
            <Button Content="查询" BorderBrush="#619CF8" BorderThickness="2" Foreground="#619CF8" FontSize="13" HorizontalAlignment="Left" Margin="659,6,0,0" VerticalAlignment="Top" MinWidth="111" Height="30" Click="query"/>
        </Grid>
        <Grid Name="Content" HorizontalAlignment="Left" Height="259" Margin="10,146,0,0" VerticalAlignment="Top" MinWidth="780" Background="White">
            <Label Content="累计库存数量：" Name="Tips" FontSize="15" HorizontalAlignment="Left" Margin="31,10,0,0" VerticalAlignment="Top" FontWeight="Bold"/>
            <ListView  VerticalAlignment="Top"  Name="listView" ItemsSource="{Binding}" HorizontalAlignment="Left" Margin="69,44,0,0">
                <ListView.View>
                    <GridView AllowsColumnReorder="true">
                        <GridViewColumn Header="商品编码" DisplayMemberBinding="{Binding goods_code}"   Width="{Binding ElementName=column1,Path=ActualWidth}"/>
                        <GridViewColumn Header="商品名称" DisplayMemberBinding="{Binding name}"    Width="{Binding ElementName=column2,Path=ActualWidth}"/>
                        <GridViewColumn Header="库存数量" DisplayMemberBinding="{Binding amount}"   Width="{Binding ElementName=column5,Path=ActualWidth}"/>
                        <GridViewColumn Header="最早失效期" DisplayMemberBinding="{Binding expire_time}"   Width="{Binding ElementName=column7,Path=ActualWidth}"/>
                        <GridViewColumn Header="操作"   Width="{Binding ElementName=column8,Path=ActualWidth}">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button x:Name="BtnConfirm" Content="库存明细" Click="onStockDetailed"  Width="75" Height="23" Tag="{Binding Path=goods_code}" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#FF0C80F3" BorderThickness="2,0,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>

                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Grid>
</UserControl>
